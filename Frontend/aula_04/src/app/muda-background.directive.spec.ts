import { MudaBackgroundDirective } from './muda-background.directive';

describe('MudaBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new MudaBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
